# core/templates/navigation/items.yaml
#
# Definiert die Framework-eigenen Navigationseinträge (erscheinen unten).
# Wird von app/templates/navigation/items.yaml ergänzt, nicht ersetzt.
#
# settings muss hier explizit aufgeführt werden wenn es erscheinen soll.

- key: sysinfo
  label: Info
  group: System

- key: settings
  label: Einstellungen
  group: System
